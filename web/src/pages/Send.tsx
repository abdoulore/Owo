// TODO(Phase 3): amount pad, note field, client-side secret generation
// (32-byte random -> keccak256 claimHash), create link, share sheet.
export function Send() {
  return (
    <main>
      <h1>Send</h1>
    </main>
  );
}
